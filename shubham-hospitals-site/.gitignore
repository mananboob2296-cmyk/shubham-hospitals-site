# Build output and local tooling — regenerated by Cloudflare Pages on every deploy.
dist/
.astro/
node_modules/

# Not tracked historically: Cloudflare resolves dependencies fresh on each build.
# Committing a lockfile is worth doing, but it changes what production installs,
# so it belongs in its own PR with its own preview check — not this one.
package-lock.json
.DS_Store
*.log
